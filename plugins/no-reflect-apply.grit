engine biome(1.0)
language js(typescript,jsx)

`Reflect.apply($fn, $thisArg, $args)` as $call where {
    register_diagnostic(
        span = $call,
        message = "Replace `Reflect.apply` with a typed function call. Model dynamic dispatch behind a named interface.",
        severity = "error"
    )
}
